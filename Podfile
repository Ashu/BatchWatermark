# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BatchWatermark' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BatchWatermark
pod 'AssetsPickerViewController', '2.6.2'
  target 'BatchWatermarkTests' do
    inherit! :search_paths
    # Pods for testing
  end

end
